{
  "name": "security-and-advanced-typing",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -c \"bgMagenta.bold,bgCyan.bold\" -p \"[{name}]\" -n \"API,WEB\" \"npm run dev -w api\" \"npm run dev -w web\"",
    "build": "npm run build -w shared && npm run build -w api && npm run build -w web"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3"
  }
}
